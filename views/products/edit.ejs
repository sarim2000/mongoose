<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Product</title>
</head>
<body>
    <form action="/products/<%= prod.id %>?_method=PUT" method="post">
        <label for="name">Product name</label>
        <input type="text" name="name" placeholder="productname" value="<%= prod.name %> ">
        <label for="number">Enter price</label>
        <input type="Number" step="any" name="price" placeholder="price" value= <%= prod.price %>>
        <label for="category">It is a</label>
        <select name="category" id="">
            <option value="vegetable" <%= prod.category === "vegetable" ? 'selected' : '' %> >vegetable</option>
            <option value="dairy" <%= prod.category === "dairy" ? 'selected' : '' %>>dairy</option>
            <option value="fruit" <%= prod.category === "fruit" ? 'selected' : '' %>>fruit</option>
        </select>
        <button type="submit">Add</button>
    </form>
    <a href="/products/<%= prod.id %> ">Cancel</a>
</body>
</html>